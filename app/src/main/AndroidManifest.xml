<!-- AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parcialeventos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Parcial Eventos"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Main Activity (Lanzador de la Aplicación) -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Ejercicio 1: TareasActivity -->
        <activity
            android:name=".TareasActivity"
            android:exported="false" />

        <!-- Ejercicio 4: ListaCompraActivity -->
        <activity
            android:name=".ListaCompraActivity"
            android:exported="false" />

        <!-- Ejercicio 6: Sistema de Tareas Avanzado -->
        <activity
            android:name=".MisTareas2Activity"
            android:exported="false" />
        <activity
            android:name=".RegistroTareaActivity"
            android:exported="false" />
        <activity
            android:name=".ListaTareasActivity"
            android:exported="false" />
        <activity
            android:name=".DetallesTareaActivity"
            android:exported="false" />

    </application>
</manifest>
